# ==============================================================================
# AIVoice TTS Framework Dependencies
# ==============================================================================
# DEPENDENCY RESOLUTION NOTES:
# - coqui-tts>=0.27.5 requires numpy>=1.26.0
# - rvc-python 0.1.5 declares numpy<=1.23.5 (overly restrictive, works at runtime with >=1.26)
# - To resolve: rvc-python is installed with --no-deps in setup.bat,
#   and its actual runtime dependencies are listed explicitly below.
# ==============================================================================

# LƯU Ý GPU: setup.bat sẽ tự phát hiện GPU và cài torch/torchvision/torchaudio
# TRƯỚC file này với index phù hợp (cu124 cho RTX 20/30/40, cu128 cho RTX 50-series).
# Index dưới đây chỉ là fallback khi cài tay bằng pip -r requirements.txt.
--extra-index-url https://download.pytorch.org/whl/cu124

# Core Machine Learning & Deep Learning Libraries
torch>=2.1.1
torchvision>=0.16.0    # Required by basicsr/realesrgan (MediaComposer upscaler)
torchaudio>=2.1.1
transformers>=4.57,<5.0

# TTS Generation Engines
edge-tts>=7.2.8        # Microsoft Edge online TTS
piper-tts>=1.2.0       # Piper offline ONNX TTS
coqui-tts>=0.27.5      # Coqui XTTSv2 voice cloning TTS (requires C++ compiler)
llama-cpp-python==0.3.31 # Local GGUF LLM text rewriting (requires C++ compiler)
vieneu==3.0.9          # VieNeu-TTS offline/online Vietnamese TTS
neucodec>=0.0.6        # Neural audio codec for VieNeu TTS voice cloning standard mode
torchao==0.16.0        # Lock to 0.16.0 to remain compatible with PyTorch 2.6.0 and prevent ImportError
                       # LƯU Ý RTX 50-series (torch cu128 >= 2.7): nếu import torchao lỗi,
                       # thử: pip install --upgrade torchao (src/main.py đã có guard chống crash)
sentence-transformers  # Required for Semantic Caching text embeddings
# NOTE: rvc-python>=0.1.5 is installed separately with --no-deps in setup.bat
#       because it declares numpy<=1.23.5 which conflicts with coqui-tts.

# Runtime dependencies of rvc-python (installed explicitly instead of via --no-deps)
fairseq==0.12.2        # Required by rvc-python (requires C++ compiler)
faiss-cpu==1.7.3       # Required by rvc-python
praat-parselmouth>=0.4.2 # Required by rvc-python
pyworld                # Required by rvc-python
torchcrepe             # Required by rvc-python
av                     # Required by rvc-python
ffmpeg-python          # Required by rvc-python
loguru                 # Required by rvc-python
fastapi                # Required by rvc-python
pydantic               # Required by rvc-python
python-multipart       # Required by rvc-python
uvicorn                # Required by rvc-python

# Pure-python / prebuilt dependencies for Coqui XTTSv2
anyascii
einops
inflect
ko-speech-tools
matplotlib
num2words
numba
pysbd
pyyaml
scipy

# Audio File Manipulation & Digital Signal Processing
soundfile>=0.12.1      # Read/write WAV files
numpy>=2.0.2           # Required >=2.0.2 by neucodec
librosa>=0.10.0        # Audio feature analysis and processing
pyloudnorm>=0.1.1      # LUFS volume normalization (required by src/utils/audio.py)

# General Utilities & CLI Helpers
python-dotenv>=1.0.0   # Environment variables loading
tqdm>=4.66.0           # CLI progress bars
requests>=2.31.0       # Model download assistant (used in download_models.py)
flask>=3.0.0           # Web UI server

# Optional Dependencies (For Vietnamese phonemization support, see README)
# git+https://github.com/vunb/viphoneme.git
# git+https://github.com/vunb/vinorm.git

# MediaComposer Integration Dependencies
moviepy==2.2.1
faster-whisper==1.2.1
openai
streamlit
opencv-python
pydub                  # Đọc thời lượng audio trong storytelling orchestrator
toml                   # Đọc/ghi config.toml của MediaComposer
imageio-ffmpeg         # ffmpeg binary cho video_assembler
pandas                 # st.data_editor / st.table trong webui (streamlit cũng kéo theo, pin rõ cho chắc)
datasets               # Import guard trong src/main.py chống crash khởi tạo torch trên Windows
pytest                 # Chạy smoke test: python -m pytest apps/MediaComposer/tests

# MediaComposer — AI Storytelling Image Generation (Workflow 5)
# LƯU Ý: thiếu nhóm này là toàn bộ tính năng sinh ảnh sập khi pull sang máy mới.
diffusers==0.38.0      # Stable Diffusion pipeline + IP-Adapter
accelerate>=1.0.0      # CPU offload / device map cho diffusers
peft>=0.19.0           # LoRA adapter (Hyper-SD, FaceID LoRA, LoRA nhân vật)
safetensors            # Load trọng số an toàn
compel>=2.0.0          # Prompt dài >77 token (chunk & concat embedding)
insightface==1.0.1     # Trích face embedding (engine faceid — ảnh người thật)
onnxruntime-gpu==1.20.2 # Runtime cho InsightFace trên GPU
basicsr==1.4.2         # Kiến trúc RRDBNet cho RealESRGAN (setup.bat sẽ patch torchvision import)
realesrgan==0.3.0      # AI Upscaler 4x
gfpgan==1.3.8          # Phụ trợ khôi phục mặt của realesrgan
